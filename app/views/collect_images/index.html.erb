<div class="row">
  <div class="col-md-10">
    <h1>Collections</h1>
  </div>
  <div class="col-md-2">
    <h1>
      <%= link_to 'New Collect', new_collect_image_path,  class: "btn btn-default btn-md" %>
    </h1>
  </div>
</div>


<table>
  <thead>
    <tr>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @collect_images.each do |collect_image| %>
      <tr>
        <td><%#= collect_image.image %></td>
        <td><%#= link_to 'Show', collect_image %></td>
        <td><%#= link_to 'Edit', edit_collect_image_path(collect_image) %></td>
        <td><%#= link_to 'Destroy', collect_image, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>
